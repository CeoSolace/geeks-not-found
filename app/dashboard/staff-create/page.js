export default function StaffCreatePage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Create Staff Account</h1>
      <p>Form for creating new staff, admin or read-only accounts. Include fields for name, username, email, role and temporary password. Force password change on first login.</p>
    </div>
  );
}